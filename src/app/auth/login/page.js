import Login from "../../components/Login"

export const metadata = {
  title: "Login",
  description: "Login page for authentication of the user.",
};

const page = () => {

  return (
    <>
      <Login />
    </>
  )
}

export default page